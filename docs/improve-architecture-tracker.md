# improve-architecture Tracker

Current product-map entry index: 148
Last updated: 2026-08-05T00:00:00Z

- 2026-08-05: feat-connectors-github → partial, improve-architecture (index 150): **RESOLVED 3 known gaps** in the GitHub connector. (1) Rate-limit budget awareness — new `_RATE_LIMIT_HEADERS` + `_parse_rate_limit_reset()` + `_rate_limit_metadata()` + `_rate_limit_detail()`; every query result now carries `metadata["rate_limit"]` (GitHub's `X-RateLimit-Limit`/`Remaining`/`Reset`/`Used`/`Resource` when present, absent → `{}`), on 429 `_retry_delay()` waits for the `X-RateLimit-Reset` quota-window instead of blind backoff, and exhausted-429 error strings surface the quota headers; all query paths routed through a new `_result()` helper (also centralising Link-header cursor parsing). (2) Recursive file listing — new `query("tree")` via the Git Trees API (`GET /repos/{owner}/{repo}/git/trees/{ref}` with `recursive=1` by default, optional `path` prefix filter + `ref` default `"main"`). (3) Path traversal protection — new `_validate_path()` (rejects absolute paths + `..` segments) wired into `query("file")`, `query("tree")` path filter, and `write("file")`. Added 17 unit tests (`test_github.py`: tree happy/recursive-false+ref/path-prefix/missing-repo/traversal, file read+write traversal ×4 parametrised, rate-limit metadata on list + single-resource + empty-fallback; `test_github_resilience.py`: reset-window wait, elapsed-reset fallback, exhausted-429 quota detail) + 2 BDD scenarios in `github_connector.feature` (recursive tree listing, path traversal on file write blocked) with 2 new step definitions + mock connector extended. Updated product map `connectors/github-connector.md` (4 behaviours `[ ]`→`[x]`, 3 Known Gaps → RESOLVED, BDD count 16→18, QA History). 80/80 `test_github.py` + 16/16 `test_github_resilience.py` unit tests + 18/18 github_connector BDD scenarios pass (pre-existing 18 connector-suite BDD failures unrelated to GitHub), ruff + format clean. Status: partial (no OAuth flow, fine-grained PAT, scope verification, structured error types, circuit breaker, batch file ops remain).

- 2026-08-04: feat-connectors-slack → covered, improve-architecture (index 149): **RESOLVED the final 2 known gaps, completing the Slack connector gap programme** (`connectors/slack/__init__.py`). (1) Bot-in-channel verification — `health_check()` now verifies the bot is in at least one channel via a new `_is_bot_in_channel()` helper (`conversations.list`, `limit=1`, `types=public_channel,private_channel`); no channels → `HealthResult(ok=False, "Bot is not in any channel")`, network failures → "Channel membership check failed due to network error", API errors → "Channel membership check failed". (2) Domain-specific exception types — new `SlackError` (base, `ValueError`-compatible) hierarchy: `SlackAPIError` (business `ok:false` + invalid JSON), `SlackRateLimitError` (429 exhausted), `SlackAuthError` (401/403 + failed `auth.test`), `SlackNetworkError` (timeout/connection/other HTTP); new `_error_for_status()` helper maps `HTTPStatusError`→type in one place, `health_check` now discriminates on exception types instead of string-matching messages, and the duplicate retry branch in `_call_api` was removed. Added 13 unit tests (`test_slack.py`: health bot-in-channel ok / bot-not-in-channel / membership network + api error; exception hierarchy matrix, 429/401/403/500/timeout/ok:false/invalid-json/verify-scopes type assertions) + `test_health_check_ok` updated for the new probe + 2 BDD scenarios in `slack_connector.feature` (health passes in-channel, health reports bot-not-in-any-channel) with 2 new step definitions. Updated product map `connectors/slack-connector.md` (3 behaviours `[ ]`→`[x]`, 2 Known Gaps → RESOLVED, BDD count 38→40, `status: partial`→`covered`, QA History). 145/145 slack unit tests + 40/40 slack BDD scenarios pass (8 pre-existing connector-suite BDD failures unchanged), ruff clean, mypy strict clean. Status: **covered** — no documented Slack connector gaps remain.

- 2026-08-04: feat-connectors-slack → partial, improve-architecture (index 148): **RESOLVED the "No scheduled-message list/delete" known gap** in the Slack connector. (1) Scheduled-message listing — `query("scheduled_messages")` (`chat.scheduledMessages.list`, optional `channel` filter, `q.cursor`→`cursor` param, records = `scheduled_messages`, `next_cursor` from `response_metadata`). (2) Scheduled-message deletion — `write("scheduled_message_delete")` (`chat.deleteScheduledMessage`, `channel` + `scheduled_message_id` required, `ValueError` on missing, `_check_slack_ok` verified). Added 10 unit tests (`test_slack.py`: scheduled_messages happy/channel-filter/cursor-pagination/api-error/http-error, scheduled_message_delete happy/missing-channel/missing-id/api-error/http-error) + 4 BDD scenarios in `slack_connector.feature` (list scheduled messages, list without channel, delete scheduled message, delete-without-id error) with 2 new step definitions + mock connector extended. **Fixed pre-existing bug** in shared BDD step `step_slack_query_unknown` (`I query resource "{resource}"`) which discarded the query result on success — the bare query step can now assert records. Updated product map `connectors/slack-connector.md` (2 behaviours `[ ]`→`[x]`, 1 Known Gap → RESOLVED, BDD count 34→38, QA History). 132/132 slack unit tests + 38/38 slack BDD scenarios pass (18 pre-existing connector-suite BDD failures unchanged), ruff clean, mypy strict clean. Status: partial (bot-in-channel verification, domain-specific exceptions remain).

- 2026-08-04: feat-connectors-slack → partial, improve-architecture (index 147): **RESOLVED 6 behaviours / 4 known gaps** in the Slack connector. (1) Message search — `query("message_search")` (`search.messages`, required `query` filter, optional `sort` `score`/`timestamp`, `count` from `q.limit`, cursor→`page` numeric with `ValueError` on non-numeric, paging-derived `next_cursor`). (2) Scheduling — `write("schedule_message")` (`chat.scheduleMessage`, `channel` + `post_at` required). (3) File uploads — `write("file_upload")` (`files.upload` multipart, `filename` + exactly one of `content`/`file`, optional `channels`/`initial_comment`/`thread_ts` as form data). (4) History pagination — `query("messages")` + `query("thread_replies")` now forward `q.cursor` to `conversations.history`/`conversations.replies`; optional `types` filter (message-type filtering) added to `query("messages")`. Added 22 unit tests (`test_slack.py`: message_search happy/multi-page/cursor+sort/missing-query/invalid-cursor/api-error/http-error, messages cursor+types, thread_replies cursor, schedule_message happy/missing-post_at/missing-channel/api-error/http-error, file_upload content/bytes/missing-filename/missing-content/both/api-error/http-error) + 6 BDD scenarios in `slack_connector.feature` (search, search-without-query error, schedule, schedule-without-post_at error, upload, upload-without-content error) with 7 new step definitions + mock connector extended. Updated product map `connectors/slack-connector.md` (6 behaviours `[ ]`→`[x]`, 4 Known Gaps → RESOLVED, BDD count 28→34, QA History). 121/121 slack unit tests + 34/34 slack BDD scenarios pass (19 pre-existing connector-suite BDD failures unchanged), ruff clean, mypy strict clean. Status: partial (bot-in-channel verification, domain-specific exceptions, scheduled-message list/delete remain).

- 2026-08-04: feat-connectors-slack → partial, improve-architecture (index 146): **RESOLVED 8 behaviours / 4 known gaps** in the Slack connector. (1) User operations — `query("user_presence")` (`users.getPresence`, `user` filter → `{user, presence, online}`), `query("user_profile")` (`users.profile.get`, `user` + optional `include_labels`, full profile incl. custom fields), `query("user_lookup")` (`users.lookupByEmail`, `email` filter) — all `ValueError` on missing required filter. (2) Message operations — `write("ephemeral_message")` (`chat.postEphemeral`, `channel`+`user` required), `write("message_update")` (`chat.update`, `channel`+`ts` required), `write("message_delete")` (`chat.delete`, `channel`+`ts` required), remaining fields passed through for rich blocks. (3) Channel operations — `write("channel_join")` (`conversations.join`), `write("channel_leave")` (`conversations.leave`), `write("channel_archive")` (`conversations.archive`), `write("channel_unarchive")` (`conversations.unarchive`). Added 30 unit tests (`test_slack.py`: presence/profile/lookup happy-path + missing-filter + api-error, ephemeral/update/delete happy + missing-field + api-error, join/leave/archive/unarchive happy + missing-channel + api-error) + 10 BDD scenarios in `slack_connector.feature` (presence, profile, lookup-by-email, ephemeral, update, delete, join, archive, unarchive, ephemeral-without-user error) with 7 new step definitions + mock connector extended. Updated product map `connectors/slack-connector.md` (8 behaviours `[ ]`→`[x]`, 4 Known Gaps → RESOLVED, BDD count 18→28, QA History). 99/99 slack unit tests + 28/28 slack BDD scenarios pass (18 pre-existing connector-suite BDD failures unchanged), ruff clean, mypy strict clean. Status: partial (file uploads, message search, channel-history pagination, bot-in-channel verification, domain exceptions, scheduling remain).

- 2026-08-04: feat-connectors-gitlab → partial, improve-architecture (index 145): **RESOLVED 3 known gaps** in the GitLab connector. (1) Recursive directory listing — new `query("tree")` (`GET /projects/{id}/repository/tree`) with optional `path`/`ref`/`recursive` filters, paginated via the existing `X-Next-Page` cursor contract. (2) Batch file operations — new `write("files")`/`write("commit")` using the Commits API (`POST /projects/{id}/repository/commits`) to apply `create`/`update`/`delete`/`move`/`chmod` actions atomically in one commit (action-type whitelist, `ValueError` on empty/invalid actions, missing `file_path`, or `move` without `previous_path`; every `file_path`/`previous_path` runs through the path-traversal guard). (3) MR approval request — new `write("mr_approval_request")` creates an approval rule (`POST .../merge_requests/{iid}/approval_rules`, `rule_type: "approval"`) requesting approval from specific `user_ids`/`user_emails` (`ValueError` when neither is given; optional `name`/`approvals_required`). Added 18 unit tests (`test_gitlab.py`: tree params + next_cursor + traversal + missing-project, batch commit body/custom branch+message/move/empty-actions/invalid-action/missing-file_path/move-traversal + previous-path-traversal, approval request by ids / by emails / no-users / missing-project) + 5 BDD scenarios in `gitlab_issues.feature` (recursive tree nested entries, batch write commit id, batch path traversal blocked, approval request approvers, no-users error) with 7 new step definitions + mock connector extended. Updated product map `connectors/gitlab-connector.md` (3 behaviours `[ ]`→`[x]`, Known Gaps 4→2, QA History). 148/148 gitlab unit tests + 5/5 new BDD scenarios pass, ruff clean, mypy strict clean. Status: partial (individual scope probing + pre-run ConnectorHub blocking, self-hosted discovery remain).

- 2026-08-03: feat-connectors-gitlab → partial, improve-architecture: **RESOLVED 4 known gaps** in the GitLab connector. (1) MR diff/changed files — new `query("mr_changes")` (`GET /projects/{id}/merge_requests/{iid}/changes`), returned as `records[0]["changes"]` alongside MR title/source/target. (2) X-Request-Id reporting — new `_request_id()`/`_error_detail()`/`_id_suffix()` helpers; the final `_call_api` ValueError appends `(request_id: <id>)` on 4xx/5xx and exhausted retries, and every health-check failure detail carries the request id when present. (3) Self-hosted version reporting — `health_check()` does a best-effort `GET /version` probe on self-hosted instances (appended as `(GitLab <version>)`), strictly non-fatal on 403/404/network failure; hosted gitlab.com is not probed. (4) Path traversal protection — new `_validate_path()` rejects absolute paths and `..` segments on `query("file")`, `write("file")`, `write("file_delete")` before any request is sent. Added 10 unit tests (`test_gitlab.py`: mr_changes records + missing-iid, path-traversal on query/write/file_delete, absolute-path rejection, nested relative path allowed; `test_gitlab_resilience.py`: X-Request-Id on 500/exhausted-429/health-401, self-hosted version detail, version-probe 403 + network-error non-fatal, hosted-no-probe) + 3 BDD scenarios in `gitlab_issues.feature` (query MR changes, health reports instance version, path traversal on file write blocked) with 2 new step definitions + mock connector extended. Updated product map `connectors/gitlab-connector.md` (6 behaviours `[ ]`→`[x]`, Known Gaps 6→4, QA History). 146/146 gitlab unit tests pass, ruff clean. Status: partial (MR approval requests, individual scope probing + pre-run ConnectorHub blocking, recursive/batch file ops, self-hosted discovery remain).

- 2026-08-03: feat-connectors-linear → partial, improve-architecture: **RESOLVED the final 2 known gaps** in the Linear connector, completing the feature-gap programme for this entry. (1) Dedicated assignment — `write("issue_assign")` accepts `{"id", "assigneeId": "<id>"}` (direct), `{"id", "email": "..."}` or `{"id", "name": "..."}` (resolved via a new Linear `users(first: 1, filter: ...)` GraphQL query, `ValueError` when no user), or `{"assigneeId": null}` / `{"unassign": true}` to clear the assignee; new `write("issue_unassign")` (`{"id"}`) clears via a single `issueUpdate`. (2) Name-resolution quality — exact (case/punctuation-insensitive via `_normalize_name()`) match first, fuzzy token-containment fallback (`_fuzzy_matches()`), with clear `ValueError` on duplicate exact names or ambiguous fuzzy matches (shared `_resolve_entity_by_name()`); state lookup now paginates through all pages via new `_STATE_LOOKUP_QUERY` + `_team_states()`/`_team_named_entities()` (mirroring the existing paginated cycle lookup). Added 14 unit tests (`test_linear.py`: assign by id/email/name/unassign-flag/null-assignee, assign missing id + missing user reference + user-not-found + update failure, unassign + missing id, fuzzy state "progress"→"In Progress", punctuation-insensitive "in-progress", duplicate exact state names raise, ambiguous fuzzy cycle name raises, exact-beats-fuzzy) + 5 BDD scenarios in `linear_connector.feature` (assign by assignee id / by email / by name, unassign, missing-user-reference error) with 5 new step definitions in `test_connectors.py` and the mock connector extended. Updated product map `connectors/linear-connector.md` (2 behaviours `[ ]`→`[x]`, both Known Gaps → RESOLVED, BDD count 24→28, QA History). 91/91 `test_linear.py`+`test_linear_resilience.py` unit tests + 28/28 linear BDD scenarios pass (pre-existing 18 connector-suite BDD failures unchanged), ruff clean. Status: partial (only cross-cutting gaps remain — expired-key vs network-error distinction, per-operation permission checks, GraphQL query-complexity limits).

- 2026-08-03: feat-connectors-jira → partial, improve-architecture: **RESOLVED 3 known gaps** in the Jira connector. (1) Dedicated assignment — `write("issue_assign")` accepts `{"issue_key", "account_id"}` (direct `accountId`), `{"issue_key", "email"}` or `{"issue_key", "display_name"}` (resolved via `GET /user/search?query=...&maxResults=1`, `ValueError` when no user / no `accountId` returned), or explicit `{"account_id": null}` / `{"unassign": true}` to clear the assignee (PUT `{"fields": {"assignee": null}}`). (2) Label management — `write("issue_label")` accepts `{"issue_key", "add": [...]}`, `{"remove": [...]}`; because Jira's `labels` field is a *set* (PUT replaces the whole list), new `_compute_target_labels()` fetches the issue's current labels and computes the add/remove target applied atomically in one PUT. (3) Issue delete — `write("issue_delete")` (`DELETE /issue/{issue_key}`, success confirmation). Added 14 unit tests (`test_jira.py`: assign by account_id / email / display_name / unassign null / unassign flag / missing key / no-identifier error / user-not-found, labels add+remove / add-dedupe / missing add/remove / missing key, delete + missing key) + 4 BDD scenarios in `jira_connector.feature` (assign, unassign, add/remove labels, delete) with 8 new step definitions in `test_connectors.py` and the mock connector extended to mirror the new resources. Updated product map `connectors/jira-connector.md` (4 behaviours `[ ]`→`[x]`, Known Gaps 6→3, QA History). 49/49 `test_jira.py` + 27/27 `test_jira_resilience.py` unit tests pass, 13/13 jira BDD scenarios pass, ruff clean. Status: partial (Jira Data Center, attachments, field metadata remain).

- 2026-08-03: feat-connectors-linear → partial, improve-architecture: **RESOLVED 3 known gaps** in the Linear connector. (1) Label assignment on issues — `write("issue_label")` accepts `{"id", "addLabelIds": [...]}` and/or `{"id", "removeLabelIds": [...]}`; because Linear's `issueUpdate.labelIds` is a *set* (replaces the full list), `_update_issue_labels()` fetches current label IDs via new `_ISSUE_LABEL_IDS_QUERY` and computes the target set so the operation is a true add/remove applied atomically in one `issueUpdate` (missing issue → `ValueError`). (2) Cycle read-back — `cycle { id name }` added to `_ISSUE_FIELDS` so all issue reads/searches/writes return the current cycle. (3) Issue archive/delete — `write("issue_archive")` (`issueArchive(id, trash)`, optional `{"trash": bool}`) and `write("issue_delete")` (`issueDelete(id)`), both with success-flag checks. Added 14 unit tests (`test_linear.py`: cycle read-back, issue_label add/remove/add+remove/missing-fields/issue-not-found/update-failure, issue_archive default/trash/missing-id/failure, issue_delete/missing-id/failure) + 6 BDD scenarios in `linear_connector.feature` (add label, remove label, no-label-ids error, archive, archive-to-trash, delete) with 7 new step definitions in `test_connectors.py` and the mock connector extended. Updated product map `connectors/linear-connector.md` (5 behaviours `[ ]`→`[x]`, Known Gaps 4→2, BDD count 18→24, QA History). 78/78 `test_linear.py`+`test_linear_resilience.py` unit tests + 6/6 new linear BDD scenarios pass (pre-existing 19 connector-suite BDD failures unchanged), ruff clean. Status: partial (issue assign/unassign helper + fuzzy state/cycle name matching remain).

- 2026-08-03 (review fix): Branch Fixer Bot addressed PR #565 review nits in the Linear connector. Renamed `_CYCLE_ARCHIVE_QUERY` → `_CYCLE_LOOKUP_QUERY` and added cursor pagination to `_resolve_cycle_id()` so a team with >100 cycles can't miss a name match. Hardened `_resolve_state_id()`/`_resolve_cycle_id()` to raise a clean `ValueError` ("Linear team {id} not found") when a nonexistent teamId returns a null `team` instead of an `AttributeError`. Whitelisted `LabelCreateInput` keys (`name`, `teamId`, `color`, `description`, `parentId`) in `write("label")` instead of passing the entire payload through. Added 4 unit tests (label input whitelisting, state/cycle team-not-found, cycle name match across pages). 56/56 `test_linear.py` unit tests pass, ruff clean.

- 2026-08-03: feat-connectors-linear → partial, improve-architecture: **RESOLVED 3 known gaps** in the Linear connector. (1) State transitions by name — `write("issue_state")` accepts `{"id", "state": "<name>", "teamId"}` and resolves the workflow state name to an ID via `_resolve_state_id()` (case-insensitive, error if not found) before applying `issueUpdate`; also accepts `{"id", "stateId"}` directly. (2) Label management — `write("label")` (`labelCreate`, `name`+`teamId` required), `write("label_update")` (`labelUpdate`, `id` required), `write("label_delete")` (`labelDelete`), all with success-flag checks. (3) Cycle/sprint assignment — `write("issue_cycle")` accepts `{"id", "cycleId"}` (direct, incl. `null` to remove) or `{"id", "cycle": "<name>", "teamId"}` resolved via `_resolve_cycle_id()`. Added 15 unit tests (`test_linear.py`: state by name/by ID/missing/not-found, cycle by name/by ID/remove/missing, label create/update/delete/missing/failure) + 10 BDD scenarios in `linear_connector.feature` (state, cycle, label, missing-team error) with 10 new step definitions in `test_connectors.py` and the mock connector extended to mirror the new resources. Updated product map `connectors/linear-connector.md` (9 behaviours `[ ]`→`[x]`, 3 Known Gaps → RESOLVED + 4 new documented, BDD count 8→18, QA History). 52/52 `test_linear.py` unit tests + 10/10 new linear BDD scenarios pass (pre-existing 19 connector-suite BDD failures unchanged), ruff clean. Status: partial (issue archive/delete, label-assignment helper, cycle read-back on issues, fuzzy name matching remain).

- 2026-08-02: feat-connectors-github → partial, improve-architecture: **RESOLVED 6 known gaps** in the GitHub connector. (1) Merge PR — `write("pr_merge")` (`PUT /repos/{owner}/{repo}/pulls/{pull_number}/merge`, optional `commit_title`/`commit_message`/`merge_method`/`sha`). (2) Get PR diff — `query("pr_diff")` (`GET /repos/{owner}/{repo}/pulls/{pull_number}` with `Accept: application/vnd.github.v3.diff`, returned as `records[0]["diff"]`). (3) Request PR review — `write("pr_review_request")` (`POST .../pulls/{pull_number}/requested_reviewers`, `reviewers` and/or `team_reviewers`, at least one required). (4) Add PR labels — `write("pr_label")` (`POST .../issues/{pull_number}/labels`). (5) Search issues — `query("search_issues")` (GitHub Search API `GET /search/issues`, required `q` filter, optional sort/order/state/labels/assignee/created/updated, `total_count` + Link-header pagination). (6) Assign issue — `write("issue_assign")` (`POST .../issues/{issue_number}/assignees`). Added 19 unit tests (`test_github.py`: pr_diff text + Accept header, search_issues records/empty/pagination/optional params, pr_merge body variants, pr_review_request reviewers/team/both/missing, pr_label, issue_assign, missing-filter/data validation) + 6 BDD scenarios in `github_connector.feature` with 5 new step definitions in `test_connectors.py`. **Fixed 2 pre-existing broken BDD scenarios** ("Create a pull request", "Comment on a pull request" — referenced steps that didn't exist in the step file). Updated product map `connectors/github-connector.md` (6 behaviours `[ ]`→`[x]`, Known Gaps 22→16, QA History). 61/61 `test_github.py` + 139/139 github connector unit tests pass, ruff clean. Status: partial (OAuth flow, fine-grained PAT, scope verification, rate-limit budget, circuit breaker, recursive/batch file ops, path traversal remain).

- 2026-08-02: feat-connectors-jira → partial, improve-architecture: **RESOLVED** known gap "No X-RateLimit-* header inspection — rate-limit retry is blind (no remaining/quota tracking)". Jira Cloud reports quota state via `X-RateLimit-Limit` / `X-RateLimit-Remaining` / `X-RateLimit-Reset` (epoch) headers on every response. Added `_RATE_LIMIT_HEADERS`, `_parse_rate_limit_reset()` (epoch → wait delay; missing/invalid/elapsed → `None`), `_rate_limit_detail()`, `_rate_limit_metadata()` and a `_jitter()` helper to `connectors/jira/__init__.py`. `_call_api` now prefers `X-RateLimit-Reset` on HTTP 429 via new `_sleep_delay()` (tight jitter around the quota window, mirroring the GitLab connector) instead of blind backoff, and the final exhausted-429 error surfaces the quota headers. Every query result now carries `metadata["rate_limit"]` (absent → `{}`). Added 16 unit tests (`test_jira.py`: rate-limit metadata on issue/search/projects, empty fallback; `test_jira_resilience.py`: reset parse ×4, quota detail summary ×2, metadata-only-present-headers, sleep-delay reset-vs-backoff, 429+reset retry-to-success, exhausted-429 quota detail) + 2 BDD scenarios in `jira_connector.feature` with step definitions in `test_connectors.py` (metadata exposed on results, quota detail in error). Updated product map `connectors/jira-connector.md` (behaviours `[ ]`→`[x]`, Known Gaps 7→6, QA History). 62/62 jira unit tests pass, 2/2 new BDD scenarios pass, ruff clean (10 pre-existing connector-suite BDD failures unchanged). Status: partial (Jira Data Center, attachments, field metadata, assign/labels/delete ops remain).

- 2026-08-02: feat-connectors-gitlab → partial, improve-architecture: **RESOLVED 4 known gaps** in the GitLab connector. (1) Self-hosted GitLab support — `GitLabConnector.__init__` accepts optional `base_url` (default `https://gitlab.com/api/v4`, trailing-slash normalised), used by `_client()`; wired through `connector_hub._build_connector()` + `polling._build_polling_connector()` via `config.get("base_url", …)` mirroring the existing `gitlab_ci` pattern. (2) File deletion — new `write("file_delete")` (`DELETE /projects/{id}/repository/files/{path}`, `ref` default `main`, optional `commit_message`). (3) MR operations — new `write("mr_note")`, `write("mr_merge")` (optional `squash`/`merge_when_pipeline_succeeds`), `write("mr_labels")`. (4) RateLimit-* header inspection — added `_rate_limit_metadata()` + `_RATE_LIMIT_HEADERS`, and an additive `metadata: dict` field on the shared `ConnectorResult` dataclass (`connectors/base.py`); every query result now carries `metadata["rate_limit"]` with GitLab's `RateLimit-Limit/Remaining/Observed/Reset/ResetTime` when present (absent → `{}`). Added 12 unit tests (`test_gitlab.py`: rate-limit metadata on list + single-resource, empty fallback, self-hosted routing for health-check + query, default base URL, `file_delete` success/default-ref/missing-filters, `mr_note`, `mr_merge`+squash, `mr_labels`). Updated product map `connectors/gitlab-connector.md` (5 behaviours `[ ]`→`[x]`, 2 new rate-limit behaviours, Known Gaps 5→3, QA History). 35/35 gitlab + 14/14 resilience + 47/47 gitlab-issues unit tests pass, ruff clean. Status: partial (scope verification, MR approval/diff, recursive/batch file ops, path traversal protection, `X-Request-Id` reporting remain).

- 2026-08-02: feat-connectors-gitlab → partial, improve-architecture: **RESOLVED** known gaps "No self-hosted GitLab support", "File deletion unimplemented", and "No RateLimit-* header inspection" + partial on "MR operations limited" and "Scope verification incomplete". Self-hosted: added `base_url` constructor arg (default `https://gitlab.com/api/v4`, trailing slash rstripped) to `GitLabConnector`, wired through `connector_hub._build_connector` (`config.get("base_url", ...)` — same pattern as `gitea`/`gitlab_ci`) and `polling._build_polling_connector`. File deletion: added `write("file_delete")` — `DELETE /projects/{id}/repository/files/{path}` with `branch` (default `main`) + optional `sha`. MR ops: added `write("mr_merge")` (`PUT .../merge_requests/{iid}/merge`, optional `squash`/`merge_commit_message`/`should_remove_source_branch`), `write("mr_approve")` (`POST .../merge_requests/{iid}/approve`), `write("mr_comment")` (`POST .../merge_requests/{iid}/notes`). RateLimit: `_call_api` now prefers GitLab's `RateLimit-ResetTime` header (quota-window wait) over blind backoff on 429 via new `_retry_delay()` helper, and `_rate_limit_detail()` surfaces `RateLimit-Limit`/`RateLimit-Remaining`/etc. in the final 429 error. Scope verification: health check now distinguishes expired tokens (401 on `/user`) from missing scopes (403 on `/user` → needs read_user/api; 403 on `/projects` → read_api/api not granted), reporting the denied endpoint/scope in `detail`. Updated product map `connectors/gitlab-connector.md` (behaviours `[ ]`→`[x]`, Known Gaps rewritten, QA History). Added 13 unit tests + 4 BDD scenarios with step definitions. 99/99 gitlab unit tests + 32/32 gitlab BDD scenarios pass, ruff clean. Status: partial (MR diff/labels/approval-request, write_repository/api scope probing + pre-run ConnectorHub blocking, X-Request-Id, version reporting, recursive dir listing, batch file ops remain).

- 2026-08-02: feat-connectors-gitlab → partial, improve-architecture: **RESOLVED** known gap "No pagination — `query("projects")` and `query("mrs")` don't return `next_cursor`". Added `_parse_next_page()` (reads GitLab `X-Next-Page` header; absent or `"0"` → `None`) and `_paginate_params()` (`ConnectorQuery.cursor` → GitLab `page` param, non-numeric cursor → `ValueError`) in `connectors/gitlab/__init__.py`. Wired into all list resources: `projects`, `mrs`/`merge_requests`, `issues`, `labels`, `milestones`, `issue_notes`, `issue_discussions`, `branches`, `tags`, `pipelines`, `jobs` (single-record resources still return `next_cursor=None`). Added 8 unit tests (`test_gitlab.py`: next_cursor echo ×4, `"0"`→`None` boundary, cursor→`page` forwarding, invalid cursor → `ValueError`, single-resource no cursor) + 2 BDD scenarios in `gitlab_issues.feature` (paginated query returns next page cursor, last page has no cursor) with new `When … on page "{page}"` step + `Then` next-cursor steps. **Fixed 2 pre-existing broken BDD scenarios** (error-on-write, error-on-query — referenced steps that didn't exist in the step file): added `When the GitLab API returns 500 on issue creation` / `returns 429 on issues query` + `Then the write/query result is an error with "{message}"` steps. Updated product map `connectors/gitlab-connector.md` (behaviours `[ ]`→`[x]`, pagination Known Gap → RESOLVED + QA History). 23/23 gitlab unit tests + 6/6 new/fixed BDD scenarios pass (pre-existing 21 connector-suite failures unchanged), ruff clean. Status: partial (5 known gaps remain — self-hosted base URL, file deletion, MR ops, scope verification, RateLimit-* header inspection).

- 2026-08-02: feat-core-polling-trigger + feat-core-trigger-system → partial, improve-architecture: **RESOLVED** known gap "Trigger-level `daily_spend_limit` is enforced at fire time but not exposed via the trigger CRUD/polling-config API — set only via the DB or admin paths". Exposed `daily_spend_limit` across the trigger API: accepted on `TriggerCreate` (`POST /pipelines/{id}/triggers`), `TriggerUpdate` (`PUT /triggers/{id}`), and `PollingConfigUpdate` (`PATCH /triggers/{id}/polling`) — validated `ge=0` (negative → 422), explicit `null` clears via `model_fields_set`, omitted `None` leaves unchanged — and echoed as `daily_spend_limit` on every trigger response (`list_triggers`, `create_trigger`, `update_trigger`, `restore_trigger`, `update_polling_config`, `list_pipeline_triggers`) via new `_serialize_spend_limit()` helper in `api/routes/triggers.py`. Also extended the MCP `create_trigger` tool with `max_concurrent_runs` + `daily_spend_limit` params (validated, echoed in response). Added 7 unit tests in `test_triggers_endpoint.py` (create echo + constructor arg, negative → 422, update set/clear, polling-config set/clear, list echo). Updated product map `core/polling-trigger.md` + `core/trigger-system.md` (behaviours `[ ]`→`[x]`, Known Gap → RESOLVED, QA History; verified `daily_spend_limit=0` blocks all cron runs). ruff clean. Status: partial (queue-depth mechanism remains open).

- 2026-08-02: feat-core-polling-trigger → partial, improve-architecture: **RESOLVED** known gap "Per-pipeline daily spend limit prevents polling run creation" (also listed in `feat-core-trigger-system`: "Daily spend limit applies to cron triggers only — polling has no spend limit check"). `fire_polling_trigger` now enforces `trigger.daily_spend_limit` via new `_daily_spend_limit_reached()` helper in `core/trigger_engine/polling.py` — scoped to trigger_id + org + today's runs, `>=` comparison, `None`-limit short-circuit, checked right after the concurrency check and before the connector query (over-budget trigger stops polling the external service). On limit reached: `spend_limit_reached` TriggerEvent logged (limit + today's cost), `next_fire_at` advanced via `_update_next_fire_no_last` (prevents re-fetch every 30s beat tick), returns `{"status": "skipped", "reason": "spend_limit", ...}`. **Wired up orphaned BDD** — `triggers/polling.feature` (9 scenarios, zero step defs) rewritten to 9 fully-executable fire-path scenarios + 3 spend-limit scenarios; created `backend/tests/bdd/steps/test_polling_triggers.py` (mirrors `test_agent_signal.py` fire-path pattern). Added 5 unit tests (`TestDailySpendLimit` in `test_polling.py`: reached skip + event + advance, `today_cost == limit` boundary, below-limit fires, no-limit fires, spend SQL scoping) and updated `_make_trigger`/`_setup_session_for_polling` helpers. Updated product map `core/polling-trigger.md` + `core/trigger-system.md` (behaviours `[ ]`→`[x]`, Error Handling/TriggerEvent Audit/Edge Cases sections, Known Gaps RESOLVED, `bdd:` + `unit-tests:` frontmatter, QA History). 63/63 polling unit tests + 9/9 polling BDD scenarios pass, ruff clean. Status: partial (queue-depth mechanism + trigger-level spend-limit API exposure remain).

- 2026-08-02: feat-core-schema-diff → partial, improve-architecture: **RESOLVED** known gap "Circular rename chains (A→B, B→A) — may produce unpredictable results". Fixed two real defects in `core/schema_registry/migration.py`: (1) rename *detection* was non-deterministic — `create_migration` iterated hash-randomised set order for removed/added candidates, so the same schema pair produced different rename plans across runs/PYTHONHASHSEED (verified: 6 different outputs); now sorts candidate names so same-type greedy matching is stable and reproducible. (2) rename *application* lost data on cycles — `apply_migration` on a plan with `{A:B, B:A}` returned `{'A': 1}` (dropping B) instead of swapping; added `_detect_rename_cycles()` (cycle extraction, returns `[['A','B']]` for A↔B) and `_apply_renames()` which applies cycles as value rotations (swap for 2-node, forward rotation for N-node) and chains tail-first (reverse topological) so a value moving into a field that is itself renamed is forwarded before overwrite. Added 11 core unit tests (`test_schema_migration.py`: swap preserves both fields, 3-node rotation, chain forwarding, determinism across 50 runs, sorted-name pairing, `_detect_rename_cycles` matrix ×5) + 1 BDD scenario + plural `fields` step definitions in `schema_migration.feature`. Updated product map `core/schema-diff.md` (Edge Case `[ ]`→`[x]`, Known Gap → RESOLVED, QA History). 89/89 core schema-migration tests + 32/32 schemas endpoint tests + 8/8 BDD scenarios pass, ruff clean. Status: partial (2 gaps remain — unaudited stateless `/migrate/plan` + no degraded fallback for partial chains; `/migrate/plan` auth dependency also still open).

- 2026-08-01: feat-core-schema-diff → partial, improve-architecture: **RESOLVED** known gap "No audit logging for schema migration operations" — `POST /api/v1/schemas/migrate` now appends a `schema_migration_completed` audit event (resource_type `schema`, resource_id = target schema; payload: from/to schema ids, `dry_run` flag, plan-change counts), recorded for both apply and dry-run, with graceful `ProgrammingError` (table missing → log + continue) and failure-isolation (audit append error never breaks the migration response). **Fixed BROKEN BDD suite** — `schema_migration.feature` was failing 4/4 scenarios on step-text mismatches (feature used `fields "name" (string)` prose that can't be `json.loads`-ed against the step definitions); rewrote scenarios to the JSON-inline step contract, removed `target_fixture` from `given` steps (pytest-bdd 8.1.0 `_register_fixture(nodeid=...)` incompatibility with pytest 9.1.1 `PytestRemovedIn10Warning` escalation), and added 2 new scenarios (apply-transforms-data + audit event, dry-run + audit event with `dry_run: true`). Added 4 route unit tests (`test_schemas_endpoint.py`: audit event recorded with correct payload, dry-run audit payload, audit failure isolation, ProgrammingError on append → 200) + 3 core scale tests (`test_schema_migration.py`: 1000/1500-field diff + apply). Updated product map `core/schema-diff.md` (Error Handling/Edge Cases/Security `[ ]`→`[x]`, Known Gaps RESOLVED + documented, QA History). 116 focused tests pass (6 BDD + 78 core + 32 route). Status: partial (3 known gaps remain — circular rename cycles, unaudited stateless `/migrate/plan`, no degraded fallback for partial chains).

- 2026-08-01: feat-teams-team-isolation → partial, improve-architecture: Implemented the long-documented **`connector_team_mismatch`** gap (PRD §9.3) — team-private connector instances are now only bindable to pipelines owned by the same team, blocked at the pipeline-save command layer with HTTP 409. Added `core/team_visibility.py` (pure `connector_team_mismatch()` rule + `find_connector_team_mismatches()` async DB check + `extract_connector_bindings()` + `connector_team_mismatch_detail()` message builder) and wired it into `api/routes/pipelines.py` on `PATCH /pipelines/{id}/graph` and `PATCH /pipelines/{id}` (with `graph_json`; uses effective owner team when the PATCH reassigns ownership) plus both MCP binding paths (`update_pipeline_graph`, `bind_connector_to_node`) so the isolation boundary can't be bypassed via MCP. Added 16 core unit tests (`tests/unit/core/test_team_visibility.py`: rule matrix, detail message, empty/invalid bindings, same-team OK, org connector OK, cross-team mismatch, team connector on org pipeline, missing connector, cross-org connector) + 5 route tests (`test_pipelines_endpoint.py`: 409 block on graph replace, same-team 200, missing pipeline 404, 409 block on update-with-graph, effective-owner-team wiring) + 5 MCP tests (`tests/unit/mcp/test_team_binding_enforcement.py`) + fixed the previously-broken `cross_team_isolation.feature` BDD suite (5 scenarios failed on step-text mismatches; now all pass, and the binding step uses the real `connector_team_mismatch()` rule). Updated product map `teams/team-isolation.md`, `auth/team-rbac.md`, `teams/team-ownership.md` ([ ]→[x], Known Gaps RESOLVED, QA History). 61 focused + 2138 full-suite tests pass, ruff clean. Status: partial (2 gaps remain — view_as_team JWT enforcement, stage_team_mismatch).

- 2026-08-01: feat-evals-eval-regression-alerts → covered, improve-architecture: **RESOLVED** known gap "No configurable recent window ratio (hardcoded at `max(days // 4, 1)`)" — added `recent_window_ratio: float = 0.25` to `detect_regressions()` (`core/eval_engine/regression.py`), validated (`>0` and `<=1.0`, else `ValueError`), with a 1-day floor and the existing half-period fallback when the recent window would span the whole period. Exposed `recent_window_ratio` as a `>0–1.0` query param on `GET /api/v1/admin/evals/regressions` (default 0.25, echoed in `RegressionAlertsResponse`). Added 11 unit/endpoint tests (default-ratio equivalence to legacy `days//4`, custom-ratio window shift, 1-day clamp, ratio=1.0 fallback, invalid-ratio `ValueError` ×3, endpoint echo, endpoint 422 ×3). Updated product map (behaviours `[ ]`→`[x]` in Lookback split / Query parameters / Response shape, gap moved to QA History RESOLVED) and frontend contract `schema.ts`. 27/27 `test_eval_regressions.py` tests + 20/20 `test_evals_endpoint.py` pass. Status: covered.
- 2026-07-31: feat-model-backends-hub → partial, improve-architecture: Verified 2 stale product-map claims against code and resolved them. **RESOLVED — Hub wired into run execution**: `executor.py._init_model_backend_hub()` (added 2026-07-09) loads active backends for the org, initialises `ModelBackendHub`, and sets it on the ContextVar; `node_runner.py._node()` resolves the backend via `hub.get(backend_id)` and invokes it (covered by `test_pipeline_composition.py`). **RESOLVED — failover audit events**: `get()`/`get_with_rotation()` emit `model_failover` audit events on fallback (added 2026-07-07, tested in `test_failover.py`). **Fixed code gap** — `get_with_rotation()` scan-all path silently omitted the `model_failover` audit event when no configured fallback was healthy and the hub scanned all registered backends; refactored the audit call into `ModelBackendHub._emit_failover_event()` used by all three rotation paths (get, configured-fallback, scan-all). Added 3 unit tests (`test_failover.py`: scan-all audit event, `get()`/`get_with_rotation()` audit-logger-failure isolation) + BDD coverage (`model_backends/hub.feature`, 7 scenarios exercising the hub directly — healthy-primary, configured fallback, all-unhealthy error, failover audit event, scan-all rotation + audit, not-found, one-decrypt-per-run). Populated `bdd:` frontmatter on the hub entry. Updated `model-backend-management.md` (documented the 3 legacy alpha BDD feature files `configure/rotation/health_check` as a known gap — placeholder `pass` steps, stale patch paths, non-existent API paths; pre-existing `test_rate_limiting.py` StepDefinitionNotFound failures unchanged). 43/43 hub unit tests + 307/307 model-backend/hub unit tests + 32/32 active model-backend BDD scenarios pass. Status: partial.
- 2026-07-31: feat-observability-error-tracking → partial, improve-architecture: Implemented `condition_window_seconds` in alert evaluation (was a documented feature gap — field stored/validated/exposed but never evaluated; rules fired off group *lifetime* count). Added `AlertEngine._count_events_in_window()` — per-fingerprint `error_events` count within the window compared against `condition_min_count`; `0`/`None` falls back to lifetime count; window-count query failures log `alert.window_count_failed` and skip only that rule (fail-safe). Added 6 unit tests (`TestConditionWindow` in `test_error_alerting.py`: threshold met/below, window=0 fallback, query-failure skip, per-rule independence, org+fingerprint scoping) + 3 BDD scenarios + step definitions in `error_notifications.feature`. Updated product map (checkbox `[ ]`→`[x]`, known gap RESOLVED, BDD count 4→7, QA History). 222/222 error_tracking unit tests pass; 3/3 new BDD scenarios pass (7 pre-existing ingestion/dashboard BDD failures unchanged, not caused by this change). Status: partial.
- 2026-07-31: feat-core-navigation-restructure → partial, improve-architecture (frontend): Verified 20 previously-unchecked Error Handling / Edge Case / Security behaviours against code (malformed-manifest → empty sidebar, unknown sidebar_group console.warn+skip, routeConfigMap fallback, JWT claim defaults, localStorage fallback via vueuse, fetchPlan failure handling, missing sidebar_order skip, empty-whitelist canSeeItem, exact-route activation, systemAdminOnly group gating, devMode visibility gating, breadcrumb cycle protection). Fixed broken ADR reference in the entry (pointed at non-existent `017-navigation-sidebar-restructure.md`; ADR 017 is the Celery→SAQ migration). Aligned entry with current flat-list design (expandable sub-menus removed in #373 — stale subItemConfig behaviours rewritten to the actual code). Added 26 new Vitest tests: `SidebarNav.spec.ts` (10: group rendering, systemAdminOnly, role/tier gating, plan-not-loaded, private_preview devMode, all-filtered groups, collapse persistence), `SidebarGroup.spec.ts` (4: toggle emit, aria-expanded, slot visibility, active styling), `SidebarLink.spec.ts` (3: link render, visibility badges, exact active state), `navigation-errors.spec.ts` (3: malformed manifest, unknown group warn+skip, missing sidebar_order), breadcrumb cyclic-parent test, and empty requiredRoles/requiredPermissions/null-restriction edge cases. Populated `unit-tests:` frontmatter (7 files). 53/53 navigation-related tests pass. Status: partial (no BDD — frontend has no pytest-bdd harness; CommandPalette/SidebarFooter untested).

- 2026-07-31: feat-admin-housekeeping → gap→partial, cross-cutting QA (index 91): Verified all 20 behaviours against code and marked [x] (16 scan categories not 8, RLS scoping, admin-only 403, savepoint group deletes, FK isolation, MCP runner/operator scopes, all 7 UI behaviours). Fixed CRITICAL frontend-backend contract mismatch: UI sent `entity_type: <category>` (e.g. `orphan_secrets`) but the cleanup API only accepts model entity types (`secret`, `connector`, …) — cleanup from the UI always returned "Unknown entity type". Added `entity_type` to scan candidates (core `_CATEGORY_TO_ENTITY` map + route `CandidateItem` + frontend `doCleanup`), added per-candidate `entity_type` behaviour + Error Handling section (8 checkboxes). Added 15 route unit tests (auth 401/403, 200 shape, 501/503/500, cleanup success/unknown-type/FK isolation) + 10 core service tests (entity_type enrichment, scanner isolation, map completeness) + 6 BDD scenarios + step definitions. Fixed `check-bdd-coverage.py` to also scan co-located step files under `features/` (34 placeholder features remain intentionally uncovered). 36/36 tests pass. Status: gap→partial (6 known gaps documented).
- 2026-07-31: product-map walk → fixed 54 entries: Fixed dead-code bug in `tools/graph-validate.ps1` section 5 (`^code:` never matched → code-path validation never ran). Repointed it at the already-parsed `codePaths` list, making the CODE check live. Fixed 15 broken `code:` refs across 13 entries (v2 migration squash renames: `0001_initial_schema`/`0002_rls_policies`/`0015_org_deletion`/`0025_team_visibility_rls`/`0027_hitl_claim_team`/`0014_team_cost_attribution`/`0012_variant_groups`/`0008_otel_config`/`0062_add_integration_tier` → `0001_v2_identity_org`/`0002_v2_teams_library`/`0003_v2_pipeline_runtime`/`0005_v2_features_system`; deleted workflows `alpha-exit-report.yml`/`publish-images.yml`/`docker-build.yml`; moved run-retention cleanup into `api/main.py`+`db/crud/run.py`; wildcard code refs → real directories; website doc path `variants/`→`evals/`). Fixed 82 stale `unit-tests:` refs (test-suite reduction collapsed `*_programming_error.py`/`*_sqlalchemy_error.py`/`*_exception_guard.py` into `test_error_handling.py` and `*_bdd.py` unit tests into `backend/tests/bdd/steps/*.py`). Marked deleted `alpha-exit-report.yml` workflow behaviours `[x]`→`[ ]` and updated Known Gaps. Removed UTF-8 BOMs from `housekeeping.md`+`_index.md`. Regenerated `_index.md` (141 entries, orphaned Admin section moved into the index, stale PRD refs fixed). Graph validates clean.
- 2026-07-31: feat-observability-monitoring-config → partial, improve-architecture (index 91): Fixed MAJOR — removed duplicated `@router.get`/`@router.put` decorators in `admin_monitor_config.py` (GET & PUT) that double-registered the routes, making `handle_db_errors` dead code. Same fix applied to `admin_triggers.py.list_trigger_events`. Added 13 backend unit tests for monitor-config (200 shape/defaults/merge, auth 401/403, 422 validation, 501/503/500 error paths). Added BDD feature file (8 scenarios) + step definitions. Populated `bdd:` + `unit-tests:` + `depends-on: feat-core-system-config` frontmatter. Verified 4 behaviours and added Error Handling section. 13/13 unit tests pass, 8/8 BDD scenarios pass. Status: partial (per-backend field validation remains).
- 2026-07-10: feat-observability-error-forwarders → partial, cross-cutting QA (index 90): First cross-cutting QA pass. Enriched product map from 10→13 verified [x] behaviours, populated bdd: + unit-tests: + depends-on: frontmatter. Added BDD feature file (12 scenarios), 29 unit tests (all 3 endpoints, error paths, edge cases). Confirmed all 3 routes consistently handle ProgrammingError→501, SQLAlchemyError→503, Exception→500. Added 4 new known gaps (per-type config validation, test-forwarder DB save race, redactable audit events, no type-specific config schema). Merged qa/feat-observability-error-forwarders to main (no bump).
- 2026-07-03: feat-core-contribution-provenance → partial, cross-cutting QA (index 89): Merged feat-qa-contribution-provenance-87 to main at v0.1.21. Fixed frontmatter (bdd:[]→8 real files, unit-tests:[]→13 real files). Marked 21 behaviours [ ]→[x] (data model, registry protocol, copy-to-adapt, ratings, BDD scenarios). Added Error Handling section (14 checkboxes). Created website docs stub. Status: partial (10 known gaps remain).
- 2026-07-03: feat-frontend-feedback-routing → partial, cross-cutting QA (index 88): Merged feat-qa-frontend-feedback-routing-87 to main at v0.1.20. Fixed CRITICAL frontend-backend contract mismatch (triggerCorrection sent wrong action), added annotation field + migration, updated product map. 57/57 tests pass. Status: partial.
- 2026-07-02: feat-pipelines-run-trace-observability → partial, cross-cutting QA (index 58): Marked all 50+ implemented behaviours [ ]→[x] (OTel bridge lifecycle mapping, token usage, span naming, export configuration, run trace ID, per-node token consumption, cost display, execution trace UI, credential safety, BDD coverage). Added `set_run_context(org_id, pipeline_id)` to LangGraphOtelBridge. Wired set_run_context call into PipelineExecutor. Updated known gaps. 47/47 tests pass. Status: partial.
- 2026-07-02: feat-core-audit-trail → partial, cross-cutting QA (index 48): added 8 missing dispatched event types to product map (pipeline.node.convert_to_agent, pipeline.node.revert_to_manual, sso_provider.created/deleted/toggled/updated, schema_inference_completed, team_deleted), added BDD placeholder steps gap, marked BDD wrong-event-types known gap as [RESOLVED], fixed 4 placeholder @then BDD steps with assertions against request.node metadata, added ProgrammingError catches (501 Not Implemented) to all 4 audit API routes. Status: partial.
- 2026-07-02: feat-core-api-versioning → partial, cross-cutting QA (index 47): marked 4 behaviours [x], added 9 unit tests for DeprecationHeaderMiddleware, fixed middleware _registry initialization bug. 9/9 tests pass. Status: partial.
- 2026-07-02: feat-core-schema-system → partial (was gap), cross-cutting QA: enriched product map from stub to comprehensive entry (12 BDD refs, 13 unit-test refs, 8 code paths, 60+ behaviour checkboxes). Expanded deletion protection scope to check PipelineSnapshot (schema_pins_json) and LibraryPrimitive (content_json) references. Added `force=true` query parameter to DELETE endpoint. Added 2 unit tests. Status: gap→partial.

## In-flight
- (none)

## History
- 2026-07-01: feat-connectors-gitlab → complete, cross-cutting QA: fixed frontmatter (added missing gitlab_issues.feature BDD ref, added test_gitlab.py and test_gitlab_issues.py unit test refs), marked 2 [ ]→[x] (scope probing during health check, limit parameter in project queries), added Error Handling section with 5 behaviour checkboxes, added _require_filter helper + health_check httpx.RequestError catch to fix raw KeyError crashes and network error crashes, added 2 BDD error-path scenarios + 5 step definitions, added 4 unit tests (network error, timeout, missing project filter, missing project data), consolidated stale known gaps: removed 'No BDD scenarios' and 'No unit tests' (both exist). 15/15 unit tests pass. Status: partial.
- 2026-07-01: feat-auth-team-api-keys → complete, cross-cutting QA: marked 13 [ ] behaviours → [x] (admin role rejection, runner/operator scope enforcement, role enforcement path, team_id admin requirement, MCP config endpoint, RLS context, 404 not-found, role restriction on update, name min length 1, nullable expires_at/revoked_at, team RBAC toggle), called `_validate_team_key_role` in `create_api_key`/`update_api_key` (resolves dead code known gap #93), added `expires_at` to `ApiKeyUpdate` model + endpoint propagation, updated known gaps (BDD coverage status, new expires_at gap, dead-code gap now resolved). Status: partial (2 major known gaps remain: MCP team_id propagation, RLS team isolation). 13/13 unit tests pass; 1 pre-existing endpoint test collection error unchanged.
- 2026-07-01: feat-connectors-schema-inference → complete, cross-cutting QA: updated product map from gap→partial — marked 11 behaviours [x], added accurate frontmatter (code paths, BDD refs, unit tests), consolidated 7 known gaps (removed 3 outdated: 'no implementation', 'BDD placeholder', 'no unit tests'), added 4 new gaps, added audit event dispatch to inference endpoint (schema_inference_completed), added connector-type validation (400 on unsupported types), added BDD scenario + step definitions for unsupported-type rejection. 11/12 BDD tests pass (1 pre-existing). Status: gap → partial.
- 2026-07-01: feat-connectors-linear → complete, cross-cutting QA: fixed frontmatter (added unit-tests), removed outdated known gaps #7 (BDD placeholder → 5 real scenarios) and #8 (unit tests exist), added 3 BDD error-path scenarios + step definitions, added 4 unit tests (missing id, update failure, GraphQL error), fixed search to respect q.limit via first:$limit, consolidated gaps from 9→9 with updated descriptions. 17/17 unit tests pass; 11 pre-existing BDD failures unchanged.
- 2026-07-01: feat-auth-sso-provider-ui → complete, cross-cutting QA: added audit event dispatching to SSO provider CRUD (create/update/delete/toggle), added duplicate name check on create (409 Conflict), added FeatureGate wrapper to SettingsSsoView.vue, added bdd:/unit-tests: frontmatter, marked 3 behaviours [ ]→[x] (FeatureGate, duplicate name, SAML 402), fixed SAML status 403→402 in product map, added 4 new known gaps (no admin CRUD BDD, nav entry not gated, no SAML integration test, no enterprise gate on nav entry)
- 2026-07-01: feat-core-saml-integration → complete, cross-cutting QA: fixed frontmatter (added bdd:, depends-on: feat-auth-jwt-auth/feat-teams-team-isolation, test_sso_saml_bdd.py), marked 60+ behaviours [x] (all SAML behaviours implemented in code), removed outdated BDD gap (feature file exists at tests/bdd/features/auth/sso_saml.feature), added 2 new known gaps (no integration test for SAML ACS real XML parsing, login route missing explicit check)
- 2026-07-01: feat-auth-rate-limiting → complete, cross-cutting QA: fixed trigger_pipeline 60/min rate marked [x]→[ ] (not implemented), rewrote BDD feature file (4→11 scenarios matching PRD §7.18), fixed in-memory TokenBucket to compute params from rule instead of hardcoded defaults, added 5 new known gaps (MCP trigger_pipeline not implemented, HITL 20/min catch-all, missing Redis integration test, BDD file location, fixed TokenBucket defaults), updated product map, updated both trackers
- 2026-06-30: feat-teams-team-isolation → complete, cross-cutting QA: fixed broken frontmatter (missing bdd:), reformatted markdown, marked 22 [ ] → [x], added 3 integration tests (set_rls_user_context GUCs, pool checkout hook reset, rls_team_isolation policy existence), added 2 BDD scenarios (set_rls_user_context error path, set_rls_user_context correctness), implemented real step definitions for 7 rls_enforcement scenarios, consolidated known gaps from 5→10 with updated descriptions, created website stub
- 2026-06-30: feat-auth-scim-provisioning → complete, cross-cutting QA: marked 34 behaviours [x], added bdd/unit-tests frontmatter, fixed 403→401 claim, added 3 new gaps
- 2026-06-30: feat-core-pipeline-execution → complete, cross-cutting QA: fixed broken frontmatter (missing bdd:), marked 5 behaviours [x] and 1 [ ], added 13 new behaviours from error path audit, created unit test file (discovered missing), added 4 BDD scenarios (empty pipeline, node returns None, runaway protection, output rejection), added 4 new known gaps (retry not implemented, DB connection lost, checkpoint migration, raised OTel verify)
- 2026-06-30: feat-evals-system → complete, cross-cutting QA: fixed broken YAML (missing bdd:), marked 24 [ ] → [x], added 20 new behaviour checkboxes from error path audit, created test_eval_engine.py (53 unit tests), fixed 3 code bugs (regex invalid pattern, LLM judge non-numeric score, regex flags support), added 3 new known gaps (JMESPath syntax errors, $ref resolution, schema depth limit), created website stub
- 2026-06-30: feat-auth-mcp-oauth → complete, cross-cutting QA: fixed broken frontmatter (missing bdd:, path mismatch), fixed BDD steps using GET→POST for authorize endpoint, added 10 new unit tests (authorize/token/consume edge cases), added 2 BDD scenarios (unknown client_id, used code), marked 60 behaviours [x], consolidated remaining gaps from 11→13 with updated descriptions
- 2026-06-30: feat-core-oidc-integration → complete, cross-cutting QA: fixed jit_provision_account→jit_provision_user rename bug, added bdd: frontmatter, fixed orphaned _index.md ref, marked 74 behaviours [x], added 11 new checkboxes from error path audit, added depends-on deps, consolidated gaps from 6→8
- 2026-06-30: feat-connectors-github → complete, cross-cutting QA: fixed broken frontmatter (missing bdd:, empty unit-tests), corrected OAuth scope names to match code, added 9 unit tests (HTTP error paths, missing filters, health check), marked 80+ behaviours [x]
- 2026-06-30: feat-teams-team-crud → complete, cross-cutting QA: fixed 3 code bugs (TeamResponse/AdminTeamItem/AdminCreateTeamResponse field mismatch created_by→account_id, update_team duplicate name check, test fixture missing get_plan_context override), added bdd: frontmatter, marked 80+ behaviours [x], added BDD feature file (team_crud.feature, 12 scenarios) + step definitions, added 4 new unit tests (duplicate rename, same-name-allowed, response account_id), consolidated gaps from 8→10 with updated descriptions
- 2026-06-30: feat-connectors-slack → complete, cross-cutting QA: added 30 unit tests, 14 BDD scenarios, 2 code fixes, fixed frontmatter YAML
- 2026-06-30: feat-auth-team-rbac → complete, cross-cutting QA: created 13 team BDD feature files (28 scenarios in rbac.feature, 11 team CRUD, 6 membership, 3 deletion, cross-team, HITL, view_as_team, stale JWT, admin override, ownership picker), marked 40 behaviours [x], documented new gaps (PATCH membership, operator self-escalation, team deletion blocking only in admin.py)
- 2026-06-30: feat-pipelines-pipeline-versioning → complete, cross-cutting QA: fixed frontmatter (added bdd:, unit-tests), added 10 BDD snapshot lifecycle scenarios, 25 step definitions, 34 behaviours verified [x], 5 UI/run-engine behaviours remain [ ]
- 2026-06-30: feat-core-notifications → complete, cross-cutting QA: fixed frontmatter (added unit-tests: []), marked 44 [ ] → [x] (claim_expired dispatch, retry + dead-letter, delivery log, admin API, expiry job, security, concurrency, backward compat), added 6 new checkboxes from error path audit (ProgrammingError catch gap on 9/11 routes, notifier dispatch_event top-level try/except, _record_delivery exception wrapping), discovered 4 BDD mismatches (X-Modulo-Signature-256 vs X-Modulo-Signature header, per-org vs per-endpoint secrets in signing.feature, 5 vs 10 failures in failure_webhook.feature, X-Modulo-Timestamp missing from code), consolidated known gaps from 6→16 with detailed descriptions
- 2026-06-30: feat-core-audit-trail → complete, cross-cutting QA: fixed frontmatter (bdd path mismatch, added missing code/unit-test refs), added 11 implemented [x] event types (dot-notation naming, actual dispatch), kept 18 PRD event types as [ ] (genuinely missing), added 20 unit tests (payload None default, request_id passthrough, invalid cursor, batch UUID filtering, has_more pagination, large payload, null date, list filtering), rewrote event_recording.feature with correct event types, added 13 new known gaps documenting PRD-vs-implementation divergence (no run_started, no hitl_claimed/approved/rejected, no team CRUD, no permission/auth/api_key events), updated improve-architecture-tracker
