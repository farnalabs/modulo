name: "CI: Bundled Runner backstop"
on:
  schedule:
  - cron: "23 3 * * 1"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  runner-ci-harness:
    name: Bundled Runner harness (weekly drift backstop)
    uses: ./.github/workflows/runner-ci-harness.yml
    with:
      force: true
    secrets: inherit
