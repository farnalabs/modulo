rules:
  - id: missing-session-begin
    patterns:
      - pattern-inside: |
          @$ROUTER.$METHOD(...)
          async def $HANDLER(...):
            ...
      - pattern-either:
          - patterns:
              - pattern: await $SESSION.execute(...)
              - metavariable-regex:
                  metavariable: $SESSION
                  regex: ^(session|db_session)$
          - patterns:
              - pattern: $SESSION.add(...)
              - metavariable-regex:
                  metavariable: $SESSION
                  regex: ^(session|db_session)$
          - patterns:
              - pattern: await $SESSION.flush()
              - metavariable-regex:
                  metavariable: $SESSION
                  regex: ^(session|db_session)$
      - pattern-not-inside: |
          async with $SESSION.begin():
            ...
    message: "Route handler with get_db_session must wrap DB ops in async with session.begin(): (autobegin=False)"
    languages: [python]
    severity: ERROR
    paths:
      include: ["**/backend/src/**"]
