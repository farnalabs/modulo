rules:
  - id: translation-newlines-entities
    pattern-regex: '&amp;|&#\d+;'
    message: "Translation values must not contain HTML entities — use the actual character or a separate translation key instead"
    languages: [json]
    severity: ERROR
    paths:
      include: ["**/frontend/src/locales/**", "**/frontend/src/i18n/**"]
