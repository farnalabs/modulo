# Global build-arg declarations — required for --build-arg from fly deploy to
# be accepted without warnings.  Re-declared inside each stage that needs them.
ARG GIT_SHA
ARG GIT_BRANCH
ARG GIT_COMMIT_TIMESTAMP
ARG GIT_COMMIT_MESSAGE
ARG BUILD_TIMESTAMP
ARG CI_JOB_URL

# =============================================================================
# Stage 1: Build frontend SPA
# =============================================================================
FROM node:20-alpine AS frontend-builder
ARG VITE_AUTO_LOGIN_USERNAME
ARG VITE_AUTO_LOGIN_PASSWORD

WORKDIR /build

COPY frontend/package.json frontend/package-lock.json ./
RUN npm install --ignore-scripts --no-audit --no-fund --ignore-platform-reqs

COPY frontend/ ./
RUN VITE_AUTO_LOGIN_USERNAME=${VITE_AUTO_LOGIN_USERNAME} VITE_AUTO_LOGIN_PASSWORD=${VITE_AUTO_LOGIN_PASSWORD} npm run build

# =============================================================================
# Stage 2: Backend + nginx for static serving
# =============================================================================
FROM python:3.12-slim

# Re-declare inside stage so the value is available to ENV instructions.
ARG GIT_SHA
ARG GIT_BRANCH
ARG GIT_COMMIT_TIMESTAMP
ARG GIT_COMMIT_MESSAGE
ARG BUILD_TIMESTAMP
ARG CI_JOB_URL

ENV GIT_SHA=$GIT_SHA
ENV GIT_BRANCH=$GIT_BRANCH
ENV GIT_COMMIT_TIMESTAMP=$GIT_COMMIT_TIMESTAMP
ENV GIT_COMMIT_MESSAGE=$GIT_COMMIT_MESSAGE
ENV BUILD_TIMESTAMP=$BUILD_TIMESTAMP
ENV CI_JOB_URL=$CI_JOB_URL

# Install nginx + uv
RUN apt-get update && apt-get install -y --no-install-recommends nginx && \
    rm -rf /var/lib/apt/lists/* && \
    pip install --no-cache-dir uv

WORKDIR /app

# Backend dependencies + source
COPY backend/pyproject.toml backend/uv.lock ./
COPY backend/src/ src/
COPY backend/alembic.ini .
RUN uv sync --frozen --no-dev --no-cache --all-extras

# Frontend build output
COPY --from=frontend-builder /build/dist/ /usr/share/nginx/html/

# nginx config
COPY deploy/fly/nginx.conf /etc/nginx/conf.d/default.conf

# Scripts for seeding
COPY scripts/ /app/scripts/

# Entrypoint + bootstrap
COPY deploy/fly/entrypoint.sh /entrypoint.sh
COPY deploy/fly/bootstrap_db.py /app/deploy/fly/bootstrap_db.py
COPY deploy/fly/cleanup_orphan_migrations.py /app/deploy/fly/cleanup_orphan_migrations.py
RUN chmod +x /entrypoint.sh

EXPOSE 8080

CMD ["/entrypoint.sh"]
