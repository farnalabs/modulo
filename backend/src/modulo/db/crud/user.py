"""CRUD for Account records — replaced User CRUD.

The ``User`` model has been replaced by ``Account`` (global) + ``OrgMembership`` (org-scoped).
Use ``modulo.db.crud.account`` and ``modulo.db.crud.org_membership`` instead.
"""
