strict-peer-dependencies=false
# legacy-peer-deps is npm-only: the typescript6 alias conflicts with
# openapi-typescript's typescript ^5.x peer range, which fails `npm ci`
# with ERESOLVE. pnpm is unaffected (its own resolution ignores this).
legacy-peer-deps=true
